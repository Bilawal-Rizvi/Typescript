function makeChai(type: string, value: number) {
    console.log(`Making ${type} chai with value ${value}`);
}


makeChai('masala', 30);
function Chai(type: string, value: number): void {
    console.log(`Making ${type} chai with value ${value}`);
}
function makingCHai(order:{type:string, value:number}):number{
    console.log(`Making ${order.type} chai with value ${order.value}`);
    return order.value
}
